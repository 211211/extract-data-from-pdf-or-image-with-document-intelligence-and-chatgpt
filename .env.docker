# =============================================================================
# Docker/Podman Environment Configuration
# =============================================================================
# Copy this file to .env when running with containers
# cp .env.docker .env
#
# All services run in containers - no global installation needed!
# Model data is persisted in Docker/Podman volumes.

# -----------------------------------------------------------------------------
# LLM Provider (Local - runs in container)
# -----------------------------------------------------------------------------
LLM_PROVIDER=mock
# LLM_PROVIDER=ollama
OLLAMA_URL=http://ollama:11434           # Container service name
OLLAMA_MODEL=phi3:mini                   # MIT licensed, ~2.3GB

# Mock mode (for testing without LLM)
# LLM_PROVIDER=mock
# LLM_MOCK_MODE=true
# LLM_MOCK_DELAY_MS=50

# -----------------------------------------------------------------------------
# Caching Layer
# -----------------------------------------------------------------------------
REDIS_URL=redis://redis:6379
MEMCACHED_SERVERS=memcached:11211

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DATABASE_PROVIDER=sqlite
SQLITE_PATH=/app/data/chat.db

# For production, use CosmosDB:
# DATABASE_PROVIDER=cosmosdb
# AZURE_COSMOSDB_ENDPOINT=
# AZURE_COSMOSDB_KEY=
# AZURE_COSMOSDB_DATABASE=chatdb
# AZURE_COSMOSDB_CONTAINER=chat

# -----------------------------------------------------------------------------
# Observability (Langfuse - Self-hosted)
# -----------------------------------------------------------------------------
LANGFUSE_ENABLED=true
LANGFUSE_BASE_URL=http://langfuse:3000
LANGFUSE_PUBLIC_KEY=pk-local-dev
LANGFUSE_SECRET_KEY=sk-local-dev

# -----------------------------------------------------------------------------
# Azure Services (Optional - for production features)
# -----------------------------------------------------------------------------
# Document Intelligence
# AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=
# AZURE_DOCUMENT_INTELLIGENCE_KEY=

# Cognitive Search
# AZURE_SEARCH_NAME=
# AZURE_SEARCH_API_KEY=
# AZURE_SEARCH_INDEX_NAME=

# Azure OpenAI (when not using local Ollama)
# LLM_PROVIDER=azure
# AZURE_OPENAI_API_BASE_URL=
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_API_DEPLOYMENT_NAME=gpt-5.1
# AZURE_OPENAI_API_VERSION=2025-04-01-preview
